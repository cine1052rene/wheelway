# WheelWay 작업 메모

- 2026-07-22: 서울·수도권 지하철 접근성 길찾기 PWA 프로토타입을 새로 구성했다.
- 이용자 유형별 제약: 목발(엘리베이터/에스컬레이터), 수동 휠체어(엘리베이터), 전동 휠체어(1,000kg 이상·문폭 90cm 이상)를 경로 엔진에 반영했다.
- 서울교통공사 편의시설 API와 국가철도공단 엘리베이터 현황을 데이터 소스로 연결할 어댑터를 만들었다. 운영 시 API 키는 서버리스 프록시에서 처리해야 한다.
- 관리자의 시설 상태·주의 공지는 현재 프로토타입에서 localStorage에 저장된다. 실서비스 전환 시 인증 및 DB 연동이 필요하다.
- `node .\\node_modules\\vite\\bin\\vite.js build --debug` 빌드가 성공했다.
- 2026-07-22 (이어서, Claude/ttapp): Codex가 `firebase deploy --only functions:facilities` 승인 대기 중 사용량 한도로 멈춘 지점을 이어받아 배포 완료. 이후 `functions/index.js`의 실제 버그 2건을 발견·수정:
  1. 서울 열린데이터광장 응답 구조가 `{getFcElvtr:{row:[...]}}`가 아니라 `{response:{header,body:{items:{item:[...]}}}}` 형태였는데 옛 파싱 코드가 `data[service].row`를 읽어 항상 빈 배열을 반환했다. → `data.response.body.items.item` 파싱 + `resultCode !== '00'` 오류 처리로 수정.
  2. `stnNm` 역명 필터가 상위 API에서 쿼리스트링/경로 세그먼트 모두 안정적으로 동작하지 않아, 전체 데이터를 페이지네이션(최대 3000건, 1000건 단위)으로 받아 서버 함수 내부에서 완전일치 우선 필터링하도록 변경.
- `https://emeraldworks.web.app/api/facilities?stnNm=강남` 실호출로 엘리베이터 4건 정상 확인 완료. API 키(`SEOUL_OPEN_DATA_KEY`)는 Firebase Secret에 정상 반영되어 있다(버전 2).
- 다음에 이어서 할 것: 프론트엔드는 아직 하드코딩된 19개 역 목데이터(`src/data/network.js`)를 사용 중 — 실제 서비스 전환 시 이 API 응답을 stations/facilities 데이터로 정규화해 반영해야 한다(README "실제 서비스 전환" 절 참고).
- 2026-07-22 (사용자 피드백 반영): "칸번호도 안내 안 해주면서 무슨 지름길이냐"는 지적을 받고 점검한 결과, 지금까지 연결한 공공데이터가 실제 경로 안내(`routeEngine.js`)에는 전혀 반영되지 않고 "데이터 정보" 탭의 연결 확인용으로만 쓰이고 있었다는 걸 확인했다.
  - 1차 개선(배포 완료): "역 접근성" 탭에 실제 엘리베이터/에스컬레이터의 출구 번호(`vcntEntrcNo`)·상세 위치(`dtlPstn`)를 보여주는 "실제 위치 정보 불러오기" 기능을 추가했다 (`publicData.js`의 `loadFacilityIndex()`, `main.js`의 `facilityListHtml`).
  - 진짜 필요한 기능(칸번호 안내)은 **서울교통공사_빠른하차정보 API**(data.go.kr, ID 15143840 — 하차역 기준 이동설비와 가장 가까운 열차 칸/출입문 번호 제공)로 해결 가능함을 확인. 단 이건 기존 서울 열린데이터광장 키(`openapi.seoul.go.kr`)와 별개로 data.go.kr(`apis.data.go.kr` 게이트웨이) 활용신청과 인증키가 별도로 필요하며, 로그인 없이는 정확한 요청/응답 스펙(Swagger)도 확인 불가 — **사용자가 직접 data.go.kr에서 활용신청 후 인증키를 전달해야 진행 가능** (사용자 액션 대기 중).
  - 데이터 정보 탭의 `dataSources`에 이 API를 "연동 예정"으로 표시해 현재 한계를 투명하게 공개했다.
- 2026-07-22 (칸번호 안내 완성): 사용자가 data.go.kr에서 활용신청 후 인증키를 전달 → Playwright로 로그인 없이 볼 수 없던 Swagger 페이지를 직접 렌더링해 실제 스펙을 확인했다.
  - **Base URL**: `apis.data.go.kr/B553766/inout`, 엔드포인트 `GET /getFstExit` (serviceKey, stnNm, dataType=JSON 등 파라미터). 응답도 기존 API와 동일하게 문서상 예시(`{header,body}`)와 달리 실제로는 `{response:{header,body:{items:{item:[...]}}}}`로 감싸져 온다는 걸 실호출로 재확인.
  - 핵심 필드: `qckgffVhclDoorNo`(칸-문 번호, 예: "8-1"), `plfmCmgFac`(인접 시설, "엘리베이터"/"계단" 등), `upbdnbSe`(상행/하행), `drtnInfo`(방면), `fwkPstnNm`(역사 내 위치).
  - 인증키는 `SEOUL_FAST_EXIT_KEY` Firebase Secret으로 저장. 새 함수 `functions/index.js`의 `quickExit` (라우트: `/api/quickExit?stnNm=역명`) 추가, `firebase.json`에 rewrite 추가.
  - 프론트: `publicData.js`의 `getQuickExit()`, `main.js`의 `findRoute()`(경로탐색 시 도착역 기준 자동 조회)와 `quickExitHtml()`(결과 화면에 "🚪 도착역 엘리베이터와 가장 가까운 칸" 카드로 상행/하행별 칸번호 노출)로 완전히 연동해 배포 완료.
  - 실제 라이브 확인: `https://emeraldworks.web.app/api/quickExit?stnNm=강남` → 16건, 서울역 → 28건 정상 응답.
  - 남은 한계: 사용자의 실제 진행 방향(상행/하행)을 앱이 자동 판별하지 않고 후보를 다 보여주므로 사용자가 방면 텍스트를 보고 스스로 골라야 한다. 추후 `routeEngine.js`의 경로 방향 정보와 매칭해 자동으로 한 줄만 추천하도록 개선 여지 있음.
- 2026-07-22 (내비게이션 UX 전면 재구성): "배치가 보고서 같다. 지상 진입→승강장→승차 칸→환승/하차 방향까지 실제 이동 순서로 알려줘야 지름길 내비게이션"이라는 피드백을 받고, 카드 나열식 결과 화면을 실제 이동 스텝 타임라인으로 갈아엎었다.
  - 새 모듈 `src/services/journey.js`: `buildJourney(originName, destName, legs)` — 경로의 각 구간을 '지상 진입 → 승차 칸 → 환승/하차 → 지상 이동' 순서로 확장하고, 필요한 실제 데이터(엘리베이터, 칸번호)를 병렬로 fetch. 엘리베이터는 `bgngFlrGrndUdgdSe`/`endFlrGrndUdgdSe` 필드로 '지상↔지하 진입용'과 '역 구내(환승) 이동용'을 구분해 분리했다.
  - `main.js`: `journeyHtml()`이 🚶진입→🚇승차(칸번호 후보)→🔀환승(구내 엘리베이터+메모)→🚇승차→🚪도착(지상 엘리베이터) 순서의 세로 타임라인을 렌더링. 기존 `quickExitHtml`/`access-note`/`steps` 요약 블록은 전부 이 타임라인으로 대체.
  - 실제 배포 후 Playwright로 라이브 검증 중 버그 2건 발견·즉시 수정: (1) 환승 메모를 `<ul>` 닫힌 뒤에 `<li>`로 이어붙여 HTML이 깨져 타임라인 밖으로 새어나가던 문제 → `<p class="journey-note">`로 변경, (2) "서울역" 같이 이미 '역'으로 끝나는 역명에 '역'이 중복 표시되던 문제("서울역역") → `stationLabel()` 헬퍼로 수정.
  - 최종 확인: 강남→서울역 검색 시 실제 화면에 강남역 1/10번 출구 엘리베이터 → 2호선 승차 칸 후보 3건 → 사당역 환승 엘리베이터+메모 → 4호선 승차 칸 후보 4건 → 서울역 2/3/4번 출구 엘리베이터까지 하나의 연결된 타임라인으로 정상 표시됨을 스크린샷으로 확인.
- 2026-07-22 (심각한 CSS 렌더링 버그 발견·수정): 사용자가 "지금 말한 것처럼 안 보인다"고 지적 → Playwright로 라이브 화면을 스크린샷+DOM 실측(`getClientRects()`)까지 해보니, `.journey-detail`/`.facility-list`(둘 다 `display:grid`, 명시적 컬럼 폭 없음) 안의 `<li>` 한글 텍스트가 박스는 정상 폭(367px)인데 실제 텍스트 줄은 ~20~30px 폭으로 쪼개져 세로로 다 무너져 있었다. `min-width:0`, `grid-template-columns:1fr` 둘 다 시도했지만 해결 안 됨 — CSS Grid의 고유 크기 계산이 한글(CJK) 텍스트의 "아무 데서나 줄바꿈 가능" 특성과 얽혀 생기는 브라우저 버그로 추정. **`display:grid`를 `display:flex;flex-direction:column`으로 교체**해서 완전히 해결(플렉스는 이 문제에서 자유로움). `.journey-steps`, `.journey-detail`, `.facility-list` 전부 flex로 전환.
  - 검증 방법을 강화: 이제부터 화면 확인은 스크린샷만 보지 않고 `document.createRange().getClientRects()`로 실제 렌더링된 텍스트 줄 폭을 직접 재서 확인한다 (스크린샷은 세로로 긴 이미지일 때 축소 왜곡으로 오판하기 쉬움).
- 2026-07-22 (역 접근성 카드 겹침 버그): 사용자가 "역 접근성 페이지 글씨가 겹쳐 보인다" 지적 → 확인해보니 `.status`(이용가능/우회 필요 칩)를 `float:right`+`margin-top:-21px`로 억지로 우측 상단에 띄우는 구식 CSS 트릭이 원인이었다. 노선 배지가 2개 이하인 역은 우연히 안 겹쳤지만, 서울역(4개 노선)·상봉(3개 노선)처럼 배지가 많아 헤더 줄이 길어지는 역은 역명(h3, `justify-content:space-between`으로 우측 정렬)과 상태 칩이 같은 우상단 자리를 두고 정면으로 겹쳤다.
  - 수정: `.station-card{position:relative}` + `.status{position:absolute;top:18px;right:18px}`로 안정적으로 고정 배치, `.station-card>div`는 `padding-right:72px`로 배지 공간을 항상 비워두고 `flex-wrap:wrap`을 추가해 배지가 많아도 줄바꿈되도록 함.
  - 검증: 19개 역 카드 전부 `h3`·`.status`의 `getBoundingClientRect()` 겹침 여부를 코드로 계산해 전부 `false`(겹침 없음) 확인 후 스크린샷으로 최종 확인.
- 2026-07-22 (로컬-원격 동기화 사고 발견·복구): 새 ttapp 세션이 `C:\project\Wheelway`를 열었더니 git 커밋이 0개인 훨씬 이전 스냅샷(mock 19개 역만, `journey.js` 자체가 없음)이었다. 실제 최신 작업은 이미 GitHub(`cine1052rene/wheelway`, 커밋 `7f3e13e`)에 전부 반영·배포돼 있었음(칸번호 안내, journey 타임라인, CSS 버그 수정 전부 포함). `git remote add origin` → `fetch` → `git reset --hard origin/master`로 로컬을 원격과 동일하게 맞춤. 빌드(`vite build`) 성공 및 라이브 API(`/api/facilities`, `/api/quickExit`) 정상 응답 재확인 완료. **주의**: 이후 세션에서도 이 폴더를 열 때 git 상태(커밋 개수, `git status`)를 먼저 확인할 것 — 로컬이 원격보다 뒤처져 있을 수 있다.
- 2026-07-22 (스크린샷 부산물 정리): `C:\project\openai` 폴더는 별개 프로젝트가 아니라 이전 세션(Codex+Playwright MCP)이 WheelWay 검증용 스크린샷을 저장하던 임시 작업 폴더였음(CLAUDE.md가 WheelWay와 동일, `.codex`/`.playwright-mcp` 캐시 존재). 스크린샷 10개와 `.codex`/`.playwright-mcp` 폴더 삭제, ttapp 프로젝트 등록에 필요한 `.claude`/`CLAUDE.md`는 보존.
- 2026-07-22 (다음 방향 논의, 코드 변경 없음): 사용자가 원래 목표는 PWA가 아니라 "앱"(안드로이드 우선, iOS 추후)이라고 확인. Flutter 전환은 백엔드(Firebase Functions)를 그대로 재사용하는 재작성 작업으로, 화면 구조가 바뀌는 항목(네트워크 확장→역 선택 UI, 관리자 인증→관리자 화면)을 먼저 끝내야 재작업이 없다고 판단해 **① 전체 수도권 네트워크 확장 → ② 관리자 인증/DB 연동(Firebase Auth+Firestore, 현재 전혀 미설정) → ③ 안드로이드 Flutter 전환** 순서로 합의. 상행/하행 자동추천(기존 데이터 구조 안 건드림)은 아무 때나 병행 가능한 별도 항목. 대략적 소요시간: Flutter MVP 포팅 8~14h, 스토어 배포 준비 +3~5h, 풀스택 백엔드(①+②+방향추천) 6.5~12.5h. 아직 실제 작업은 시작 안 함 — 다음 세션에서 ①번(전체 네트워크 확장)부터 이어갈 것.
- 2026-07-23 (라우팅 엔진 성능 병목 분석, 코드 변경 없음): 네트워크 확장(①) 착수 전, "역이 많아지면 느려지지 않냐"는 지적으로 `routeEngine.js`를 실측 분석. 병목 3가지 확인: (1) 매 반복마다 큐 전체 `sort()` — O(V²logV), (2) 매 노드 처리 시 `connections` 전체를 선형 스캔(인접 리스트 미사용) — O(V×E), 가장 큰 병목, (3) 간선 확장마다 `path` 배열 전체 복사. 해결책: 인접 리스트 사전 구축 + 바이너리 힙 우선순위 큐 + `previous` 포인터 역추적(경로는 도착 시 1회만 재구성). 예상 추가 소요 1~1.5h. **순서 합의: 엔진 최적화 → 네트워크 확장** (데이터부터 늘리면 느린 상태로 검증하다 재작업 발생 우려).
- 2026-07-23 (자동화 툴 연계 타당성 검토, 코드 변경 없음): Dify/n8n/Make.com이 개발 속도에 도움될지 검토 → **결론: 지금 병목(라우팅 엔진 계산, 네트워크 데이터 확장)엔 부적합**. 이 도구들은 "서비스 연결·자동화" 도구지 "앱 내부 알고리즘 계산" 도구가 아니라서, 오히려 왕복 지연만 추가됨. 대신 **향후 운영 단계(엘리베이터 실시간 상태 갱신·고장 알림, Dify 기반 자연어 안내 챗봇)**에는 적합 판단. 비교 검토 내용을 PPT로 문서화: `docs/reports/WheelWay_Automation_Tools_Review.pptx` (생성 스크립트 `docs/reports/generate_automation_review.py` 동봉, python-pptx 사용). 아직 git 미커밋 상태.
- 2026-07-23 (⭐ 앱 정체성 재확인 — 매우 중요, 향후 모든 작업의 기준): 사용자가 "이 앱은 도로 위를 달리는 내비게이션이 아니라 **교통약자를 위한 지름길 안내 앱**"이라고 명확히 재정의함. 지상→지하 진입, 지하철 이동(칸번호 포함), 지하→지상 진출까지 전체 동선에서 가장 빠르고 안전한 "지름길"을 제시하는 게 핵심 존재 이유. 이는 지금까지 구현한 `journey.js`(지상 진입→승차 칸→환승→지상 진출 타임라인)·`quickExit`(칸번호 안내) 방향과 일치하며, **앞으로 모든 기능/UI 결정의 최우선 기준**으로 삼아야 함:
  - 네트워크 확장(①) 시: 역만 늘리지 말고 출구 위치·엘리베이터·칸번호 데이터를 반드시 함께 채울 것. 그냥 "노선도 앱"이 되면 안 됨.
  - 라우팅 엔진 최적화 시: `transferPenalty`(휠체어 유형별 환승 페널티) 로직은 성능 개선을 이유로 절대 단순화/제거하지 말 것 — 이게 "실질적 지름길" 계산의 핵심.
  - UI/카피 톤: "경로 찾기"가 아니라 "지름길 찾기", 결과 화면은 지도 중심 내비 스타일이 아니라 지금처럼 단계별 이동 타임라인 유지.
- 2026-07-23 (라우팅 엔진 최적화 완료, 커밋 `eca24f8`): 인접 리스트(Map 캐시) + 바이너리 최소 힙 + `previous` 포인터 경로 역추적으로 리팩터. 기존 코드와 나란히 9개 시나리오(강남↔서울역·이수↔여의도·노원↔이태원 × 3프로필) 결과가 완전히 동일함을 확인 후 커밋. 착수 전 복구지점(`6a1e981`)도 GitHub에 푸시해둠.
- 2026-07-23 (⭐ ① 네트워크 확장 1단계 완료 — 1~8호선 240역, 실 데이터 기반): "역만 늘리면 안 되고 접근성 데이터가 진짜여야 한다"는 정체성 원칙에 따라, **가짜 데이터 생성 대신 실제 공공데이터로 위상(topology)까지 재구성**하는 방식을 택함.
  - **데이터 소스**: `/api/facilities?type=elevator`(엘리베이터, 실호출 865건/239역)·`?type=escalator`(에스컬레이터, 1881건/223역) — 이미 배포된 Firebase Functions를 파라미터 없이 호출하면 서울 전역 데이터가 통째로 반환됨을 발견(백엔드가 stnNm 없으면 필터 없이 전체 반환). **역명(stnNm)을 물리적 역 id로 사용**해 여러 노선 데이터가 같은 이름이면 자동으로 환승역 병합됨. **역번호(stnNo, 예 '211','211-1')로 실제 노선 순서를 복원**해 `connections`(간선)를 자동 생성 — 2호선 성수지선처럼 지선이 본선에 잘못 재합류하지 않도록 분기 처리, 2호선 순환선 closure(충정로→시청)도 반영.
  - **결과**: 1~8호선(서울교통공사 관할) 240개 역, 간선 266개. `git bash`로 BFS 연결성 검증 결과 240개 역 전부 하나의 연결 요소로 확인(고립 노드 없음). 9호선·공항철도·신분당선·수인분당선 등 타 운영사 노선은 이번 데이터에 없어 미포함.
  - **⚠️ 중요한 안전 판단 — doorWidthCm 관련**: 기존 19개 역의 `capacityKg`/`doorWidthCm`는 **전부 손으로 지어낸 값**이었다는 걸 이번에 재확인(실제 API엔 문 폭 필드 자체가 없음). 실제 장애인 이용자에게 꾸며낸 수치를 사실처럼 보여주는 건 위험하다고 판단해: `capacityKg`는 실측값(`pscpWht`) 그대로 사용하되, `doorWidthCm`는 **"법정 최소 기준(90cm) 추정치"**로 명확히 표시(`doorWidthEstimated:true`)하고 UI(`역 접근성` 탭)에도 "법정기준 추정치, 실측 아님"이라고 노출하도록 수정함. `routeEngine.js` 로직 자체는 변경 없음(null/0 비교로 자연히 안전하게 처리됨).
  - **실 데이터 검증 부수 효과**: 잠실→신촌(2호선 순환선 직행 가능 구간)을 전동 휠체어로 조회하면, 실제 데이터상 잠실나루(750kg)·구의(900kg) 엘리베이터가 1000kg 기준 미달이라 자동으로 우회 경로(천호·왕십리 경유)를 추천함을 확인 — 실데이터가 프로필별로 의미 있게 다른 경로를 만들어내는 것 검증됨.
  - **파일 구조**: `src/data/network.js`는 이제 barrel 파일(12줄, `stations.js`/`connections.js` 재노출 + `lineColors`)이고, 실 데이터는 `src/data/stations.js`(246줄)·`connections.js`(270줄)로 분리 — 500줄 제한 준수. 생성 스크립트는 `scripts/build_network.py`(재현 가능, 방법론 주석 상세 기재)로 남겨 데이터 갱신 시 재실행만 하면 됨.
  - `main.js` 기본 선택값(`gangnam`/`seoul` → `강남`/`서울역`)과 역 접근성 카드 렌더링(엘리베이터 없음/문폭 추정치 표기) 갱신. 빌드(`vite build`) 통과.
  - **남은 과제**: 9호선·공항철도 등 잔여 노선 추가, 국가철도공단 API로 실제 문 폭 데이터 확보(가능하면), 관리자 인증/DB 연동(②)으로 진행.
  - **최종 리뷰 중 발견·수정(커밋 `2aebce0`)**: `vcntEntrcNo`(출구 위치) 원본 값이 '7'/'내부'/'1,2'/'천호 방면1-2번 출구'처럼 형식이 제각각인데 전부 숫자로 가정해 "~번 출구"를 기계적으로 붙이는 바람에 240역 중 224역(93%)에서 "내부번 출구" 같은 어색/오류 문장이 생겼던 걸 배포 전에 발견해 수정함 (숫자 토큰만 "번 출구" 접미, 나머지는 원문 유지). **앞으로 build_network.py를 수정할 때는 반드시 note 문구를 몇 개 역 샘플로 육안 확인할 것.**
- 2026-07-23 (⭐ 문 폭 데이터 안전성 수정 — 새 앱 착수 전 데이터 리스크 제거): 사용자가 "목업데이터가 오류를 만들까 걱정"이라고 지적. 점검 결과 심각한 문제 발견 — `routeEngine.js`의 전동 휠체어 필터가 `doorWidthCm >= 90`을 게이트로 걸고 있는데, 실제 API엔 문 폭 필드가 없어 240역 전부 `doorWidthCm: 90`(지어낸 값)이 박혀 있어서 **이 조건이 단 한 역도 걸러내지 못하는 "가짜 통과"** 상태였다. 실제 문이 좁은 구형 역이 있어도 앱이 "괜찮다"고 안내하게 되는 위험(핵심 사용자=전동 휠체어).
  - **수정 방침(사용자 승인)**: 실측 안 된 값은 라우팅 게이팅에 절대 쓰지 않는다. 표현은 "모른다"가 아니라 사용자 요청대로 **"실측 전 / 실측 필요"**.
  - 스키마 변경: `doorWidthCm: 90, doorWidthEstimated: true` → `doorWidthCm: null, doorWidthStatus: '실측 전'` (240역, 비엘리베이터 역은 status도 null). 실제 폭이 확보되면 `doorWidthCm`에 숫자 넣고 `doorWidthStatus: null`로 두면 라우팅이 그 값을 반영하도록 설계.
  - `routeEngine.js`: `electric.allowed`를 `capacityKg >= 1000 && (doorWidthCm == null || doorWidthCm >= MIN_DOOR_CM)`로 변경 — 실측 전(null)은 통과, 실측값이 있고 기준 미달이면 차단. **회귀 검증**: electric 통과역 227 == capacity≥1000 엘리베이터역 227(문폭 게이트 무해화 확인), 강남→서울역 3프로필·잠실→신촌 전동(8호선 천호 우회로 750kg 잠실나루 회피) 모두 기존과 동일.
  - `main.js` UI: "문폭 90cm(법정기준 추정치)" → "문폭 실측 전(실측 필요)". `README.md`·`build_network.py` 주석·방법론 설명 전부 갱신. `doorWidthEstimated` 참조 저장소 전체에서 완전 제거 확인. `vite build` 통과.
  - **미측정 값은 필터에 안 쓴다 원칙**은 새 앱(Flutter/네이티브) 데이터 스키마의 기본 원칙으로 그대로 승계할 것. 다음: 국가철도공단 등 실제 문 폭 API 재조사 or 관리자 인증(②)의 "실측 제보" 필드로 점진 대체.
- 2026-07-23 (⭐ Flutter 앱 신규 착수 — 웹 포팅이 아니라 새로 빌드): 사용자가 "웹을 앱으로 바로 전환할 필요 없다, 새로 처음부터 앱 빌드"를 택함. 백엔드(Firebase Functions)·데이터는 재사용, 프론트는 접근성 우선으로 새 설계. Flutter로 결정(iOS는 추후). 진행 순서 합의: ①프로젝트 구조 → ②디자인 토큰/테마 → ③백엔드 연동 레이어 → ④화면 뼈대 → (이후)로직·실기기.
  - **구조**: 기존 저장소 안 `app/`에 Flutter 생성(`com.emeraldworks.wheelway`, Android/iOS). Flutter SDK 경로: `C:\Users\cine1\Documents\flutter`(PATH 미등록 — 빌드 시 export 필요). `flutter analyze`는 SDK dev/ 폴더 탐색 버그로 크래시하니 **`dart analyze lib`로 검증**할 것(dart 경로: `flutter/bin/cache/dart-sdk/bin`).
  - **디자인 시스템 "에메랄드 클리어"**: design_planning 에이전트룸으로 색/타이포/간격/컴포넌트/다크모드 전체 스펙 수립(보고서: `app/../memory/agent-room/agent-room-design_planning-2026-07-23T12-15-06.md`). 라이트/다크 ColorScheme 완전 정의(primary #08705B / 다크 #6FDBB8), 15단계 TextTheme, space2~64, 반경 토큰. `lib/theme/`(app_colors/app_spacing/app_typography/app_theme)로 분리. **Flutter 3.44에서 deprecated된 background/surfaceVariant/MaterialStatePropertyAll은 회피**(scaffoldBackgroundColor·surfaceContainerHighest·WidgetStatePropertyAll로 대체) — analyze 경고 0.
  - **폰트**: 가이드는 Noto Sans KR 권장하나 번들 없이 fontFamily 지정하면 조용히 폴백되므로 지금은 시스템 기본(fontFamily=null) 유지, 수치/굵기/자간만 스펙대로. **배포 전 Noto 번들 시 AppTypography.fontFamily만 바꾸면 됨**.
  - **백엔드 레이어**: `WheelwayApi`(facilities/quickExit 기존 엔드포인트 재사용), `Facility`/`QuickExit` 모델(웹 publicData.js와 동일 필드). baseUrl=https://emeraldworks.web.app.
  - **화면 뼈대**: 3탭 셸(지름길 찾기/역 접근성/데이터 정보). 역 접근성 탭은 라이브 API 실조회 동작(백엔드 재사용 즉시 검증). 지름길 찾기는 라우팅 엔진·네트워크 데이터(240역) Dart 포팅 전까지 "준비 중" placeholder.
  - **검증**: dart analyze No issues, **APK 디버그 빌드 성공**(app-debug.apk). 커밋 `01a52e4`(착수), `575b56e`(디자인 반영).
  - **다음**: 라우팅 엔진 + stations/connections(240역) Dart 포팅 → 지름길 찾기 실동작. `doorWidthCm=null(실측 전)은 게이팅 제외` 안전 원칙 그대로 승계. 이후 실기기에서 디자인 디테일·Noto 폰트·다크모드 육안 확인.
- 2026-07-23 (⭐ Flutter "지름길 찾기" 실동작 완성 — 라우팅 엔진·240역 데이터·타임라인 Dart 포팅, 커밋 `4c06823`): 웹의 routeEngine.js/journey.js를 Flutter로 이식해 앱의 핵심 기능을 완성. 에뮬레이터(Pixel6/Android15)로 강동→강남 전체 흐름 시각 검증 완료.
  - **데이터 생성**: `scripts/gen_dart_data.mjs`가 JS 데이터(src/data/stations.js·connections.js)를 Dart 소스(`app/lib/data/stations.dart`·`connections.dart`)로 자동 변환(240역/266간선). build_network.py 재실행 후 이 스크립트만 다시 돌리면 됨. connections는 Dart record 타입 `(String,String,String,int)` 리스트. **doorWidthCm=null(실측 전)은 게이팅 제외 원칙 그대로 승계.**
  - **route_engine.dart**: 다익스트라(인접리스트 캐시+바이너리 최소힙+previous 포인터 역추적), (역,진입노선) 상태키로 환승 페널티 반영(목발3/수동6/전동7). 순수 Dart 회귀검증: 강남→서울역 목발25·수동28·전동29분, 전동 통과역 227==capacity≥1000 엘리베이터역 227(문폭 게이트 무해), 잠실→신촌 전동 8호선 우회 — 웹과 동일.
  - **journey_service.dart**: 경로를 '지상진입→승차칸→환승→지상진출' 타임라인으로 확장. 엘리베이터를 bgngFlrGrndUdgdSe/endFlrGrndUdgdSe로 지상진입용/구내이동용 분리, 칸번호는 quickExit API. 모든 역 엘리베이터+칸을 Future.wait 병렬 조회.
  - **UI**: 프로필 세그먼트(목발/수동/전동), 240역 검색 바텀시트(station_picker), 결과 요약카드+세로 타임라인(journey_timeline). 출발/도착 스왑 버튼.
  - **에뮬 시각검증으로 버그 발견·수정**: (1) API 타임아웃 12→30s(escalator 조회 ~11.6s, 커밋 `7395598`). (2) off-by-one 라벨 버그 — leg.isTransfer를 '진입 시 환승'(출발 기준)으로 잡아 도착역이 환승으로 오표기됨 → buildLegs에서 `i < groups.length-1`(마지막 구간만 도착, 나머지는 환승)로 수정.
  - **⚠️ 에뮬레이터 테스트 노하우(중요)**: (a) `flutter analyze`는 SDK dev/ 폴더 버그로 크래시 → `dart analyze lib` 사용. (b) `adb shell input text`는 한글(비ASCII) 불가 → 역 검색 대신 목록에서 좌표 직접 탭하거나 ADBKeyboard 필요. (c) Android15 스타일러스 손글씨 팝업이 검색창 포커스에 끼어듦 → `adb shell settings put secure stylus_handwriting_enabled 0`로 비활성. (d) Git Bash가 `/sdcard/` 경로를 Windows경로로 변환 → screencap엔 `MSYS_NO_PATHCONV=1`, pull 로컬경로엔 미적용(별도 명령)으로 분리. 스크린샷 로컬저장은 `/c/tmp/`.
  - **남은 과제**: 관리자 인증/DB(②), 상행/하행 자동추천, 백엔드 facilities 응답 최적화(TODO), 실기기 다크모드·Noto 폰트 확인, 스토어 배포 준비.
- 2026-07-24 (⭐ 디자인기획팀 UX 구조 개선 반영 — 커밋 `b326f4c`): 사용자가 "텍스트위주·호선별 선택 안 됨·스크롤 압박" 3가지를 지적. "이건 디자인(색)이 아니라 구조 문제"라 판단해 디자인기획팀(에이전트룸)에게 UX 구조 재설계만(에메랄드 클리어 토큰은 무수정) 의뢰 → 하이브리드 컨셉(타임라인=스텝카드, 역선택=호선 2×4그리드, 입력분리=고정헤더) 채택.
  - 가이드 원문 대비 **2가지 자체 판단으로 개선**: (1) 호선 배경 위 텍스트 색은 가이드의 하드코딩 목록(`{3,6,9}`) 대신 `AppColors.onLineColor()`로 **실제 색상 휘도 계산**해 자동 판정 — 색상표가 바뀌어도 항상 대비 보장. (2) `FacilityTile` 상태 배지(운행중/점검중/고장)는 **실데이터에 없는 필드라 제외**(문폭 실측 전 원칙과 동일한 데이터 정직성 기준) — 실측 가능한 출구·정격하중만 배지화.
  - 신규 컴포넌트: `StepIconBadge`(48dp 터치, 진입/승차/환승 프리셋), `DurationLozenge`, `FacilityChip`(엘리베이터·칸번호 정보를 통일된 칩으로 — 기존엔 문장 나열이라 시각 언어 불일치했음), `TimelineStepCard`(연결선+배지+카드), `LineFilterGrid`(호선 2×4 그리드, 64dp), `RouteSummaryCard`(총 소요시간 숫자 강조), `FacilityTypeFilter`(전체/엘리베이터/에스컬레이터).
  - `route_search_screen`/`station_access_screen`: `Column(고정 헤더) + Divider + Expanded(독립 스크롤)` 구조로 리팩터 — 결과를 보다가 입력을 다시 확인하려 매번 최상단 스크롤할 필요 없어짐. `station_picker`는 `showModalBottomSheet`+고정높이 → `DraggableScrollableSheet(snap, snapSizes:[0.6,0.85])`로 전환.
  - **에뮬레이터 실기기 검증 완료**: 2호선 필터→3역만 남음(가나다순 240역 스크롤 불필요 확인), 3·6호선 카드가 자동으로 검정 텍스트(대비 계산 정확), 강남→강동구청 목발 경로에서 문장 나열이 카드+칩("출구1/1번출입구", "3-4번 칸/강변")으로 완전히 바뀜, 스크롤해도 상단 출발/도착역 필드 고정 확인, 역접근성 엘리베이터/에스컬레이터 필터 정상 동작.
  - `dart analyze` 통과. 새 파일 8개+수정 4개.
  - **다음**: ② 관리자 인증/DB, 상행/하행 자동추천, 실기기 다크모드 확인, Noto 폰트 번들, 스토어 배포 준비.
- 2026-07-24 (⭐ 노선 확장 대비 동적화 + 3·6호선 색상 분리 + 노선도 탭 선택, 커밋 `32339fb`): 사용자가 "수도권 전체노선이 8개가 전부 아님(GTX 계속 늘어남)", "채도 여전히 낮음(3·6호선 육안 구분 안 됨)", "아이콘 더 줄여줘", "노선도에서 바로 찍어 선택하는 기능" 4가지 요청.
  - **진단 정정**: 3호선/6호선은 채도 문제가 아니라 **HSL 색상각(Hue)이 27.3°/29.2°로 거의 동일**(둘 다 주황 계열)한 게 원인이었음(python colorsys로 실측). 채도만 올리면 절대 안 풀리는 문제 — 6호선만 Hue를 48°(금갈색)로 이동해 분리, 전체 채도 목표도 0.72→0.8로 상향.
  - **`lib/data/lines.dart`(신규)**: `kAvailableLines` — "1~8호선" 하드코딩 제거, kStations에서 실제 존재하는 노선을 동적 추출. 9호선·공항철도·GTX 등이 build_network.py 재실행으로 데이터에 추가되면 필터그리드·배지가 **코드 수정 없이 자동 확장**. `AppColors._rawLineFuture`에 아직 데이터엔 없는 노선(9호선/공항철도/GTX-A·B·C/신분당선/수인분당선/경의중앙선) placeholder 색도 미리 등록해둠(주석: "공식 브랜드색 확인 전 임시값").
  - **LineFilterGrid**: 64dp→48dp(접근성 최소 기준, 이보다 작게는 안 만듦)로 축소 + 고정 Row → 동적 Wrap. **실기기 버그 발견·수정**: Material/InkWell이 Wrap의 무제한폭 측정 시 가로 전체로 늘어나 "BOTTOM OVERFLOWED" 렌더링 오류 발생 → `IntrinsicWidth`로 감싸 해결(Wrap+Material 조합의 알려진 함정, 기억해둘 것).
  - **`lib/services/line_topology.dart`(신규)**: kConnections 원본 리스트 순서(build_network.py가 본선 stnNo 오름차순 뒤 지선 순으로 이미 정렬해둠)를 그대로 체인으로 이어붙여 노선별 역 순서를 복원 — **역 좌표(위경도) 데이터 없이도** 노선도 UI 구현 가능케 함. 지선·순환선(2호선)은 별도 체인으로 분리되나 완전한 지리적 정확도는 아님(주석에 한계 명시, 실제 좌표 기반 지도는 추후 별도 데이터 확보 필요).
  - **`LineMapView`(신규) + station_picker "목록/노선도" 토글**: 노선을 가로로 훑으며 역 칩을 직접 탭 → 즉시 출발/도착역 선택. 에뮬레이터로 청량리 탭 → 출발역 반영 확인 완료.
  - dart analyze 통과, 에뮬레이터 실기기 검증 완료(3·6호선 색 분리, 오버플로 없음, 노선도 탭 선택).
  - **다음**: ② 관리자 인증/DB, 상행/하행 자동추천, 실기기 다크모드 확인, Noto 폰트 번들, 실제 GTX/9호선 데이터 확보 시 build_network.py 확장.
- 2026-07-24 (역 선택 UX 3차 개선 — 커밋 `1bd43c1`): 사용자 피드백 3가지: "정사각 아닌 원형 아이콘+일렬 가로스크롤", "전체노선 버튼 작동 안 하는데 왜 있냐(기능하게 하거나 없애라)", "역 이름이 왼쪽만 있어 공간낭비 — ㄱㄴㄷㄹ은 왼쪽, 이름은 오른쪽".
  - LineFilterGrid: 정사각형 2행 Wrap → 원형 칩 한 줄 가로 스크롤(`ListView.separated`). "전체 노선" 버튼은 사실 개별 노선 재탭 해제와 기능이 겹쳐서 존재감 없었던 게 원인 — 별도 버튼 제거하고 맨 앞 원형 "전체"(Icons.apps) 칩으로 통합.
  - **GroupedStationList(신규)**: 초성(ㄱㄲㄴㄷㄸㄹㅁㅂㅃㅅㅆㅇㅈㅉㅊㅋㅌㅍㅎ) 기준으로 역을 그룹핑, 왼쪽 28dp에 초성 표시 + 오른쪽 Wrap 그리드에 그 초성 역명들 배치. 완성형 한글 유니코드(가~힣, 0xAC00~0xD7A3)는 코드포인트 순서가 이미 초성 순서와 일치해 별도 정렬 로직 불필요(`(code-0xAC00)~/588`로 초성 인덱스 계산). 화면당 노출 역 수 5개→20개 이상으로 대폭 증가(실기기 확인).
  - **⚠️ 재발 버그 패턴(중요, 기억해둘 것)**: `Wrap` 안에 `Material`/`InkWell`+`Container(alignment 포함)` 조합을 넣으면 Wrap이 측정 시 자식에게 무제한 폭을 줘서 셀이 한 줄 전체로 늘어나는 렌더링 오버플로가 발생한다. 이번에 `GroupedStationList`의 이름 칩에서 **또** 겪음(지난 세션 LineFilterGrid에서 겪은 것과 동일 패턴을 잊고 반복). **앞으로 Wrap 안에 탭 가능한 칩을 넣을 땐 항상 `IntrinsicWidth`로 감쌀 것 — 잊지 말 것.**
  - dart analyze 통과, 에뮬레이터 실기기 검증 완료(원형 칩 가로스크롤, 전체 칩 동작, 초성 인덱스 그리드 전부 확인).
  - **다음**: ② 관리자 인증/DB, 상행/하행 자동추천, 실기기 다크모드 확인, Noto 폰트 번들, 실제 GTX/9호선 데이터 확보.
- 2026-07-24 (⭐ 네이버지도 벤치마킹 반영 — 색상 미니바 + 노선 컬러 타임라인 + 전체 노선도 신설, 커밋 예정): 사용자가 "네이버지도 지하철 내비를 직접 체험해보고 반영할 점 찾아달라"고 요청 → Playwright로 강남역 2호선→서울역 1호선 경로를 실제로 조회해 UI를 스크린샷 분석(`naver1~9.png`). 발견한 패턴을 WheelWay 데이터로 구현 가능한 것만 선별 적용(실측 없는 값은 만들지 않는다는 원칙 유지).
  - **① 좌우 하차문 정보 — 조사 후 "불가"로 결론**: 네이버는 "내리는 문: 오른쪽/왼쪽"을 보여주는데, WheelWay가 쓰는 서울교통공사_빠른하차정보 API(`B553766/inout/getFstExit`) 실응답을 직접 확인(`curl .../api/quickExit?stnNm=강남`)한 결과 필드는 `qckgffVhclDoorNo`(칸-문번호, 예 "8-1")·`plfmCmgFac`·`upbdnbSe`·`drtnInfo`·`fwkPstnNm` 등으로, **좌/우 여부를 나타내는 필드 자체가 없음**을 확인. data.go.kr에 별도 "좌우하차정보" 데이터셋이 있는지도 웹검색으로 확인했으나 존재 확인 안 됨(네이버는 자체 구축 데이터로 추정). **실측 안 된 값을 만들어 쓰지 않는다는 원칙에 따라 구현하지 않음** — 향후 그런 공공데이터가 나오면 추가할 것.
  - **② `RouteSummaryCard`에 구간별 색상 미니바 추가**: `journey.legs`(노선·소요분)를 받아 승차 구간을 노선 색으로 비례 배분한 가로 바 렌더링(`_RouteMiniBar`, `route_summary_card.dart`). 네이버처럼 바 위엔 "N분", 아래엔 "N호선" 라벨 병기(FittedBox로 좁은 구간도 안 겹치게 자동 축소). 환승 구간은 실측 도보시간 데이터가 없어 임의 폭을 만들지 않고 3dp 흰 여백으로만 구분.
  - **③ 타임라인 연결선에 노선색 반영**: `TimelineStepCard`에 `lineColor` 파라미터 추가, 승차 스텝 카드 아래 연결선을 그 구간의 실제 호선색으로 표시(기존엔 전부 고정된 `primaryContainer` 색). 진입/환승 스텝은 도보 구간이라 기존처럼 중립색 유지. 네이버가 세로선 색으로 "지금 몇 호선 구간인지" 즉시 보여주는 것과 동일 효과.
  - **④ "노선도" 탭 신설(가장 큰 변경)**: "지하철 로드맵 앱인데 정작 전체 노선도가 없다"는 지적 — 기존엔 `LineMapView`가 역 선택 바텀시트 안에서만 쓰였음. 독립된 `LineMapScreen`(신규)을 만들어 하단 탭에 4번째로 추가(지름길 찾기/**노선도**/역 접근성/데이터 정보). 역 좌표 데이터가 없어 실제 지도 굴곡은 못 그리지만(기존 한계 승계), 노선별 위상 체인은 그대로 재사용. 역을 탭하면 바텀시트로 "출발역으로/도착역으로" 선택 가능 → `HomeShell`이 `_presetOrigin`/`_presetDestination` 상태를 들고 있다가 지름길 찾기 탭으로 전환하며 `RouteSearchScreen(initialOrigin/initialDestination)`으로 미리 채워 넣음(탭 전환 시 위젯이 다시 생성되는 기존 구조를 그대로 활용 — `ValueKey`로 프리셋이 바뀔 때마다 새로 반영).
  - **에뮬레이터 실기기 검증**: 시청→강동(목발) 조회 → 미니바가 2호선(초록, 14분)·5호선(보라, 16분) 비율대로 정확히 렌더링, "5호선 승차" 카드 배지도 보라색으로 일치, 노선도 탭에서 시청역 탭 → "출발역으로" → 지름길 찾기 탭에 출발역 자동 반영까지 전 과정 확인. `dart analyze` 통과, 로그캣에 오버플로/예외 없음.
  - **다음**: ② 관리자 인증/DB, 상행/하행 자동추천, 실기기 다크모드 확인, Noto 폰트 번들, 실제 GTX/9호선 데이터 확보, 좌우하차 정보는 적합한 공공데이터 발견 시 재검토.
- 2026-07-24 (⭐ 입력 헤더 네이버 스타일 리팩터 — 오버스크롤 스트레치 제거 + 스위칭 버튼 겹침배치 + 프로필 한 줄, 커밋 예정): 사용자 피드백 3가지: "스크롤 끝에 화면 늘어나는 거 보기 싫다", "출발/도착 스위칭 버튼이 한 줄을 다 차지하지 않게", "네이버지도와 색상·글씨크기·배치를 최대한 비슷하게, 제목 위에 목발/수동/전동을 오른쪽으로 올려 한 줄로".
  - **오버스크롤 스트레치 제거**: 안드로이드 12+ 기본 "스트레치" 오버스크롤 효과(스크롤 끝에서 화면이 눌린 듯 늘어나는 그 효과)가 거슬린다는 지적 — `AppScrollBehavior`(신규, `theme/app_scroll_behavior.dart`)가 `MaterialScrollBehavior.buildOverscrollIndicator`를 오버라이드해 인디케이터를 완전히 제거. `MaterialApp.scrollBehavior`에 등록해 앱 전역(모든 `ListView`/시트)에 한 번에 적용.
  - **스위칭 버튼 겹침 배치**: 기존엔 `Align(centerRight)+IconButton`이 출발/도착 필드 사이에서 독립된 한 줄을 차지했음. `_RouteInputBox`(신규)로 출발·도착 필드를 테두리 하나의 박스 안에 위/아래로 붙이고(`Column`+`Divider`), 원형 `_SwapButton`을 그 경계선 위에 `Stack`+`Positioned`로 겹쳐 띄움 — 네이버지도 길찾기 입력창의 스위칭 버튼 배치와 동일한 패턴. 필드 오른쪽엔 버튼과 안 겹치게 여백(56dp) 확보.
  - **네이버 스타일 좌측 마커**: 출발역 앞엔 초록 원(`Icons.trip_origin`, `cs.primary`), 도착역 앞엔 빨간 깃발(`Icons.flag`, `cs.error`) — 네이버의 출발(초록점)/도착(빨간 깃발) 마커 관례를 그대로 차용(노선도 화면의 출발/도착 버튼 아이콘과도 통일).
  - **프로필 선택 한 줄 통합**: 기존 라벨+아이콘 `SegmentedButton`(가로로 넓어 제목과 한 줄에 못 들어감)을 아이콘 전용 48dp 원형 토글 3개(`_ProfileIconButton`)로 축소, `Tooltip`+`Semantics`로 라벨 접근성은 유지. `Row(제목 Expanded + 선택바)`로 제목과 같은 줄에 배치 — 네이버 길찾기 상단의 "교통수단 탭 한 줄" 배치 참고.
  - **에뮬레이터 검증**: 강남→강동(목발) 조회 — 헤더 한 줄(제목+프로필 3원), 입력창 하나(출발/도착+겹친 스위칭 버튼), 결과 리스트 하단까지 스크롤·플링해도 화면 늘어남/찌그러짐 없음, 로그캣 오버플로/예외 없음, `dart analyze` 통과.
  - **다음**: ② 관리자 인증/DB, 상행/하행 자동추천, 실기기 다크모드 확인, Noto 폰트 번들, 실제 GTX/9호선 데이터 확보, 좌우하차 정보는 적합한 공공데이터 발견 시 재검토.
